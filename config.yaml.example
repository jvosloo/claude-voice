# Claude Voice Interface Configuration

input:
  hotkey: "right_alt"          # Key to hold for recording
  language_hotkey: null         # Key to cycle languages (e.g. "right_cmd")
  auto_submit: false           # Press Enter after transcription
  min_audio_length: 0.5        # Ignore recordings shorter than this (seconds)
  typing_delay: 0.01           # Delay between keystrokes (seconds)
  transcription_cleanup: false # Enable LLM-based error correction
  cleanup_model: "gemma2:2b"   # Ollama model for cleanup

transcription:
  model: "large-v3"             # tiny.en, base.en, small.en, medium.en, large-v3
  language: "en"
  device: "cpu"                # cpu or cuda (for faster-whisper)
  backend: "mlx"               # "faster-whisper" or "mlx" (mlx is faster on Apple Silicon)
  extra_languages: []            # Additional languages to cycle through (e.g. ["af", "de"])

speech:
  enabled: true                # Set to false to disable TTS output
  mode: "notify"               # "notify" = short status phrases, "narrate" = read full response
  # Notify mode settings
  # notify_phrases:              # Customize status phrases (remove or match default to use shipped audio)
  #   permission: "Permission needed"
  #   done: "Ready for input"
  #   error: "Something failed"
  voice: "af_heart"            # Kokoro voice ID (see README for full list)
  speed: 1.0                   # Playback speed (1.0 = normal)
  lang_code: "a"               # Language: a=American, b=British, j=Japanese, z=Chinese, e=Spanish, f=French
  max_chars: null              # Limit spoken output (null = unlimited)
  skip_code_blocks: true       # Don't speak code blocks
  skip_tool_results: true      # Don't speak tool output

audio:
  input_device: null           # null = system default
  sample_rate: 16000           # Whisper expects 16kHz

overlay:
  enabled: true                # Show visual indicator during recording/transcription
  style: "dark"                # "dark" = dark HUD, "frosted" = blur glass, "colored" = solid color pills
  recording_color: "#34C759"   # Green (bars on dark/frosted, background on colored)
  transcribing_color: "#A855F7" # Purple (dots on dark/frosted, background on colored)